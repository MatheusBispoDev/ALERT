import 'package:flutter/material.dart';

class StrechesDetailPage extends StatelessWidget {
  const StrechesDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
